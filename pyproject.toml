[tool.black]
line-length = 80
target-version = ['py39']
# experimental_string_processing = true
include = '\.pyi?$'
exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
'''

# [tool.isort]
# line_length = 80
# src_paths = ''
# profile = 'black'

[tool.pytest.ini_options]
addopts = "--cov=savethat --cov-report html"
testpaths = ['test']
filterwarnings = [
    'ignore:.*Create unlinked descriptors is going to go away.*:DeprecationWarning',
    # https://github.com/tensorflow/tensorboard/pull/5138
    'ignore:.*is a deprecated alias for the builtin.*:DeprecationWarning',
]

[[tool.mypy.overrides]]
module = [
    "anyconfig",
    "b2sdk",
    "b2sdk.*",
    "b2sdk.v2",
    "coverage",
    "cpuinfo",
    "matplotlib",
    "pandas",
    "reproducible",
    "setuptools",
    "sklearn",
    "sklearn.*",
    "typing_inspect",
]
ignore_missing_imports = true
